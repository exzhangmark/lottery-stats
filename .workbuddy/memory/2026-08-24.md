# 2026-08-24 工作日志

## 彩票开奖统计工具 (lottery-stats)
- 需求：PHP + HTML + Nginx + Workerman 抓取双色球(ssq)/大乐透(dlt)开奖并展示号码出现次数。
- 实现：Workerman 后台 Worker 双 API 容错抓取（主 api.huiniao.top，备 caipiaodate.com），失败 1h 重试；SQLite 存储；PHP 接口 list/stats；前端双板块 33+16 / 35+12 球。
- 已 git 提交并推送到 GitHub：https://github.com/exzhangmark/lottery-stats （公开仓库，master 分支）。
- 注意：GitHub token 仅在 push 命令内联注入，未写入 .git/config（沿用 user MEMORY 约定）。
- 部署：Nginx root 指向 public/，PHP-FPM 9000；Worker 常驻 `php worker/lottery_worker.php start -d`。

## 新增：右侧意见反馈栏
- 前端：右侧固定栏 + 右上角「反馈」按钮 + 弹窗输入（昵称选填/内容必填），提交后刷新列表；前端 esc() 转义防 XSS。
- 后端：feedback 表 + feedback_list / feedback_add 接口（api/index.php）；db() 在建表时一并建 feedback 与 feedback_rate。
- 防攻击：仅 POST、参数化写入、内容≤500/昵称≤30、控制字符过滤、重复字符抑制、每 IP 60s 最多 3 条速率限制（feedback_rate 时间戳表）。
- 已 commit + push 到 origin master（66b0356）。

## 修复：意见栏无反馈入口 + 列表加载失败
- 根因1：抽屉内缺少打开提交弹窗的按钮（openModal 无入口调用）→ 抽屉内新增「我要反馈」按钮。
- 根因2：api/index.php 曾 require worker(lottery_worker.php) → 第一行 require vendor/autoload.php(Workerman)，未 composer install 时接口 500 → 列表加载失败。
- 修复：抽取 db.php（db/feedbackRateLimited/sanitizeFeedback，不依赖 vendor），api 与 worker 共用；api 不再依赖 Workerman，无需 composer install 即可跑接口。
- sandbox 内 PHP 仅 pdo_mysql 无 pdo_sqlite，无法本地起服务验证接口；服务器需 PHP 开启 pdo_sqlite（Worker 本就用 sqlite）。
- 已 commit + push（0b5f659）。

## 数据源调整：主源改为福彩官网 cwl.gov.cn
- 顺序：official(cwl.gov.cn, 仅福彩/双色球) → backup1(huiniao) → backup2(caipiaodate)。原两个 API 降为备用。
- 新增 parseCwl：解析 openCode(红球+蓝球) 及多层 result / 单条结构；httpGet 支持自定义请求头（cwl 带 Referer）；fetchLottery 跳过不含该彩种的源（dlt 在 cwl 无数据自动跳过）。
- 沙箱无法直连 cwl.gov.cn（HTTPS 握手 schannel 失败，环境限制，非代码问题），parseCwl 已用 mock 数据验证通过。
- 已 commit + push（66eb95c）。

## 新增：历史开奖回填脚本 worker/backfill.php
- 需求：采集 2026-01-01 至今开奖并入库，模拟正常访问速度（3~5s 读取一次）防限流。
- 实现：新增 lib.php 抽取公共抓取/解析（CONFIG 数据源+parse 函数），worker 与 backfill 共用，不再耦合 Workerman；backfill.php 按彩种选第一个可用源分页回填，每页 rand(3,5)s 休眠，仅写 open_time>=since，按 (type,issue) 去重，某页最新期早于 since 即停。
- 本地真跑验证（php -d extension=pdo_sqlite，--source=huiniao）：ssq 97 期 + dlt 96 期入库成功，节流与停止条件正常。huiniao 分页确认 page 递增返回更早期号（20/页，最新在前），open_time 形如 "2026-08-23 21:15:00"。
- 顺带修复 db.php 数据库路径 bug：原为 `__DIR__.'/../data/...'`（db.php 在项目根，导致库落到项目上一级目录），改为 `__DIR__.'/data/...'`。
- README 补充 cwl 主源说明与回填用法。已 commit + push（bb4ef08）。
- 注意：本地 data/lottery.sqlite 已被 .gitignore 排除，不会随仓库部署；服务器需各自 `git pull` 后运行 backfill.php 回填其本地库。
